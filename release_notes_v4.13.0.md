### ✅ 全面修复与优化

1. **修复检查更新弹窗逻辑**：
   - 设置页「前往下载」按钮 → 弹出更新对话框
   - 右下角状态栏「点此下载」 → 同样弹出更新对话框
   - 已忽略版本在状态栏仍显示点击按钮

2. **优化桌面快捷方式**：
   - 弹出「另存为」对话框让用户选择位置（默认桌面）
   - 创建后弹窗展示任务 ID 和命令行命令，带一键复制按钮
   - 快捷方式图标使用新版优化图标

3. **彻底解决快捷方式/命令行运行不了**：
   - exe 名固定为 `AutoFlow.exe`（不再带版本号）
   - 安装包新增「添加到系统 PATH」可选任务（默认勾选）
   - 安装后即可在任意命令行执行 `AutoFlow --run-task <id>`

4. **优化软件图标**：
   - 16×32px 小图标不渲染文字，只显示清晰无限符号
   - 48px 以上才显示 autoflow 文字，解决小图标文字超大的问题
   - 小图标无限符号居中显示

5. **任务编辑器 ID 强化**：
   - 任务编辑页顶部显示任务 ID（灰色小字）+「复制」按钮
   - 新增「⌨ 命令行」弹窗按钮，展示完整命令一键复制

6. **安装包优化**：
   - 安装前自动清理旧 `_internal` 目录（依赖目录）
   - 用户数据/配置/项目文件保留不受影响
   - 卸载后插件目录也不删除

[查看完整版本历史](https://github.com/XinyuCraft-XYHC/autoflow-windows)